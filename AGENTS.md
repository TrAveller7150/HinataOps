# HinataOps 开发规范

## 目标与边界

- HinataOps 是面向 AoiLearn 的运维 Agent Demo；复杂度集中在证据采集、调查编排和受控处置。
- 优先实现可由真实 AoiLearn 环境验证的能力，避免为了展示而构建未接入实际系统的抽象。
- P0 之后新增基础设施访问时，默认只读；任何写操作必须有明确的人工审批边界和验证步骤。
- 不将 SSH 私钥、数据库密码、Token 或真实环境配置提交到 Git。实际配置放在被忽略的 `config/environments/*.toml` 中，示例配置必须脱敏。

## 代码设计

- 保持模块职责单一：Agent Core 负责调查决策；Ops MCP Server 负责受限的基础设施事实采集；不要让 Core 直接连接 Docker、Redis、MySQL 或 SSH。
- MCP Tool 只接受业务参数，绝不向模型暴露任意 Shell 命令、任意 SQL 或任意容器名执行入口。
- 基础设施命令、SQL 和 API 请求必须参数化或使用固定白名单；Tool 返回值应使用 Pydantic 模型表达稳定、可验证的结构。
- 对可能产生大量数据的来源，优先返回聚合、计数和有界样本，避免把无界日志或历史记录塞入 Agent 上下文。
- 沿用 Python 3.13、`uv` 和现有目录结构；不要使用全局 Anaconda Python 或直接 `pip install`。

## 注释与文档要求

- 所有代码注释和 Python 文档字符串必须使用中文编写；技术专有名词、代码标识符和外部协议名称可保留英文。
- 每个类必须有简洁的类文档字符串，说明其职责、边界和关键协作者；不要写无信息量的“这是一个 X 类”。
- 核心业务逻辑必须有更详细的注释，解释“为什么这样判断/分类/限制”，尤其是：证据合并、根因假设更新、Tool 白名单、权限检查、审批状态转换和故障处置验证。
- 注释应解释不明显的设计决策、输入约束和失败语义，而不是逐行复述代码。
- 公开 MCP Tool、Pydantic 返回模型和配置项应具备足以供 Agent/调用方理解的文档字符串或字段描述。
- 修改系统行为、Tool 语义或故障诊断策略时，同步更新 `README.md` 或 `docs/` 中对应说明。

## 测试与验证

- 新增逻辑先定义可验证的成功标准，再实现代码。
- 为分支判断、边界条件和失败路径编写单元测试；涉及真实基础设施的能力额外做一次只读集成验证。
- 提交前至少运行 `uv run pytest`；修改 Python 源码后额外运行 `uv run python -m compileall -q src`。
- 不得通过删除测试、放宽断言或伪造 Tool 返回值让测试通过。

## 变更纪律

- 只修改当前任务直接相关的文件，不顺带重构正常运行的模块。
- 发现真实环境异常时先采集证据并报告；未经明确授权，不重启、删除、扩缩容或修改 AoiLearn 容器和数据。
- 实现完成后说明：修改了什么、如何验证、仍有哪些已知限制。
